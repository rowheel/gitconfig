[credential]
helper = store

[pull]
rebase = true

[receive]
denyCurrentBranch = warn

[core]
editor = code --wait
excludesfile = ~/.gitignore_global

[gpg]
format = ssh

[gpg "ssh"]
allowedSignersFile = ~/.ssh/allowed_signers

[commit]
gpgsign = true
